Steps to create a repo locally and push to github

- Local folder
	-- local folder created
	-- $ git init i# in the folder
	-- README.md copied from skills-introduction-to-github
	-- $ git add README.md # staged
	-- $ git commit # stages committed
	-- $ git status # all clean
- Setup email and username in local config
	-- $ git config --global user.email gyula.szabo.bfx@gmail.com
	-- $ git config --global user.name gszabo-bfx
	-- UNSET # remove entry from config
	-- git config --global --unset user.emael gyula.szabo.bfx@gmail.com
- Authenticate
	-- $ gh auth login
	-- follow on screen guide
- Create and push repo from local
	-- $ gh repo create 
	-- follow on screen guide at the end script asks if push need



